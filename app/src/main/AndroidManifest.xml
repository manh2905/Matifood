<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MATIFOOD"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".activity.dasboard.FirstScreen"
            android:exported="true"
            android:label=""
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.dasboard.MainActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.MATIFOOD">

        </activity>
        <activity
            android:name=".activity.itemlistbycategory.ItemListActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.MATIFOOD">

        </activity>
        <activity
            android:name=".activity.itemdetailscreen.DetailFoodScreen"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.MATIFOOD2" >

        </activity>
        <activity
            android:name=".activity.auth.AuthActivity"
            android:exported="true"
            android:label=""
            android:theme="@style/Theme.MATIFOOD2" >

        </activity>
    </application>

</manifest>